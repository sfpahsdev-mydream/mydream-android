# MyDream Training / Evaluation

This folder is reserved for the notebook/server-side pipeline.

Planned responsibilities:

- Read Android-exported JSONL sleep sessions.
- Generate candidate windows.
- Generate `wakeable` and `deep_soon` labels.
- Train and evaluate the first LightGBM model.
- Export model artifacts for later on-device inference tests.
